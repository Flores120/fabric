<h1>Welcome to Fabric</h1>
<div class="row">
  <div class="col-sm-6">
    {{#each model as |product|}}
      {{product-tile product=product}}
    {{/each}}
  </div>
  <div class="col-sm-6">
    {{display-cart}}
  </div>
</div>
