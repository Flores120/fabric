<h1>Welcome to Fabric</h1>
<div class="row">
  <div class="col-sm-6">
    {{#each model as |product|}}
      {{product-tile product=product destroyProduct="destroyProduct"}}
    {{/each}}
  </div>
  <div class="col-sm-6">
    <div class="col-sm-12">
        {{display-cart}}
    </div>
  </div>
</div>


{{add-item addNewProduct="addNewProduct"}}
